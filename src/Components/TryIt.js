import React from "react";
import classes from "./TryIt.module.css";

const TryIt = () => {
	return (
		<div className={classes.tryItCard}>
			<p>
				<span>Try it free 7 days </span>then $20/mo. thereafter
			</p>
		</div>
	);
};

export default TryIt;
